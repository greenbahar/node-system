package main

func main() {
	// we have a minor instance to broadcast extracted blocks to all nodes in the blockchain network
	//counter := 0
	//for {
	//	block := fmt.Sprintf("block number %d", counter)
	//	broadcastBlock(node.Block(block))
	//
	//	counter++
	//}
	type Block struct{}

}

//func broadcastBlock(block node.Block)  {
//	seeds := dnsServer.GetNodeSeeds()
//	for _, seed := range seeds{
//
//	}
//}
